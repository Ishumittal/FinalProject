import { AfterViewInit, OnDestroy, QueryList } from '@angular/core';
import { BehaviorSubject } from 'rxjs';
import { NxExpandableTableRowComponent } from './expandable-table-row.component';
import { NxExpandable } from './toggle-button.component';
import * as i0 from "@angular/core";
/**
 * The nxExpandableTable adds row expansion functionality to the nxTable.
 * It provides the logic to expand/collapse all expandable rows simultanously.
 */
export declare class NxExpandableTableDirective implements OnDestroy, AfterViewInit, NxExpandable {
    rows: QueryList<NxExpandableTableRowComponent>;
    expanded: BehaviorSubject<boolean>;
    private readonly _destroyed;
    ngAfterViewInit(): void;
    ngOnDestroy(): void;
    /**
     * Toggles the expandable rows. If all rows are open it will close them, otherwise it will open them.
     */
    toggle(): void;
    /**
     * Expands all expandable rows.
     */
    expand(): void;
    /**
     * Closes all expandable rows.
     */
    close(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<NxExpandableTableDirective, never>;
    static ɵdir: i0.ɵɵDirectiveDeclaration<NxExpandableTableDirective, "table[nxExpandableTable]", ["nxExpandableTable"], {}, {}, ["rows"]>;
}
