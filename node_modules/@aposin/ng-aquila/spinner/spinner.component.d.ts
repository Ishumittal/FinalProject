import { BooleanInput } from '@angular/cdk/coercion';
import { ChangeDetectorRef } from '@angular/core';
import * as i0 from "@angular/core";
/** Options for sizing of the spinner. */
export declare type SpinnerSize = 'small' | 'medium' | 'large';
export declare class NxSpinnerComponent {
    private _cdr;
    private _size;
    /** Sets the size of the spinner. Default is 'small'. */
    set size(value: SpinnerSize);
    get size(): SpinnerSize;
    private _negative;
    /** Whether the spinner should use a negative styling. */
    set negative(value: BooleanInput);
    get negative(): boolean;
    constructor(_cdr: ChangeDetectorRef);
    static ɵfac: i0.ɵɵFactoryDeclaration<NxSpinnerComponent, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<NxSpinnerComponent, "nx-spinner", never, { "size": "nxSize"; "negative": "negative"; }, {}, never, never>;
}
