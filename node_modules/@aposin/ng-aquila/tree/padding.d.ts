import { Directionality } from '@angular/cdk/bidi';
import { NumberInput } from '@angular/cdk/coercion';
import { CdkTree, CdkTreeNode } from '@angular/cdk/tree';
import { ElementRef, OnDestroy, Renderer2 } from '@angular/core';
import * as i0 from "@angular/core";
/**
 * Indent for the children tree dataNodes.
 * This directive will add left-padding to the node to show hierarchy.
 */
export declare class NxTreeNodePaddingDirective<T> implements OnDestroy {
    private _treeNode;
    private _tree;
    private _renderer;
    private _element;
    private _dir;
    /** Current padding value applied to the element. Used to avoid unnecessarily hitting the DOM. */
    private _currentPadding;
    /** CSS units used for the indentation value. */
    indentUnits: string;
    /** The level of depth of the tree node. The padding will be `level * indent` pixels. */
    get level(): number;
    set level(value: NumberInput);
    _level: number;
    /** The offset is added once on top of each indent. Default number is 0. */
    get offset(): number;
    set offset(value: NumberInput);
    _offset: number;
    /**
     * The indent for each level. Can be a number or a CSS string.
     * Default number 24px from material design menu sub-menu spec.
     */
    get indent(): number;
    set indent(indent: NumberInput);
    _indent: number;
    private readonly _destroyed;
    constructor(_treeNode: CdkTreeNode<T>, _tree: CdkTree<T>, _renderer: Renderer2, _element: ElementRef<HTMLElement>, _dir: Directionality | null);
    ngOnDestroy(): void;
    /** The padding indent value for the tree node. Returns a string with px numbers if not null. */
    _paddingIndent(): string | null;
    _setPadding(forceChange?: boolean): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<NxTreeNodePaddingDirective<any>, [null, null, null, null, { optional: true; }]>;
    static ɵdir: i0.ɵɵDirectiveDeclaration<NxTreeNodePaddingDirective<any>, "[nxTreeNodePadding]", never, { "level": "nxTreeNodePadding"; "offset": "nxTreeNodePaddingOffset"; "indent": "nxTreeNodePaddingIndent"; }, {}, never>;
}
