import { BooleanInput } from '@angular/cdk/coercion';
import { ElementRef, OnChanges, SimpleChanges } from '@angular/core';
import { NxIconRegistry } from './icon-registry';
import * as i0 from "@angular/core";
/** Types of icon sizes */
export declare type IconSize = 'auto' | 's' | 'm' | 'l' | 'xl';
export declare class NxIconComponent implements OnChanges {
    el: ElementRef;
    private _iconRegistry;
    /** Keeps track of the elements and attributes that we've prefixed with the current path. */
    private _elementsWithExternalReferences?;
    private _name;
    private _previousFontClasses;
    /** Sets the name for specifying the icon.*/
    set name(name: string);
    get name(): string;
    private _outline;
    /** Whether the icon has an outline. */
    set outline(value: BooleanInput);
    get outline(): boolean;
    private _fill;
    /** Whether the icon is filled. */
    set fill(value: BooleanInput);
    get fill(): boolean;
    private _size;
    /** Specifies the size of the icon. */
    set size(value: IconSize);
    get size(): IconSize;
    /** Sets the font name that should be used. */
    font: string;
    constructor(/** @docs-private */ el: ElementRef, _iconRegistry: NxIconRegistry);
    ngOnChanges(changes: SimpleChanges): void;
    private renderIcon;
    private _setSvgElement;
    private _clearSvgElement;
    private _updateFontIconClassesFromOverride;
    private _updateFontIconClasses;
    private _setFontIconClasses;
    private _removePreviousFontClasses;
    static ɵfac: i0.ɵɵFactoryDeclaration<NxIconComponent, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<NxIconComponent, "nx-icon", never, { "name": "name"; "outline": "outline"; "fill": "fill"; "size": "size"; "font": "font"; }, {}, never, never>;
}
