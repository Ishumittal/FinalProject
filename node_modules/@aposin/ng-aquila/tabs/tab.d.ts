import { BooleanInput } from '@angular/cdk/coercion';
import { AfterContentInit, EmbeddedViewRef, OnChanges, OnDestroy, SimpleChanges, TemplateRef } from '@angular/core';
import { Subject } from 'rxjs';
import { NxTabGroupBase } from './tab-group-base';
import * as i0 from "@angular/core";
export declare class NxTabComponent implements OnChanges, OnDestroy, AfterContentInit {
    private _tabGroup;
    private _label;
    private _disabled;
    /** Emits whenever the internal state of the tab changes. */
    readonly _stateChanges: Subject<void>;
    /**
     * Content for the tab label given by `<ng-template nxTabLabel>`.
     * @docs-private
     * */
    get templateLabel(): TemplateRef<any>;
    set templateLabel(value: TemplateRef<any>);
    private _templateLabel;
    /** Template inside the NxTab view that contains an `<ng-content>`. */
    _implicitContent: TemplateRef<any>;
    /**
     * Template provided in the tab content that will be used if present, used to enable lazy-loading
     */
    _explicitContent: TemplateRef<any>;
    /** Sets the label of the tab shown in the tablist. */
    get label(): string;
    set label(value: string);
    /** Sets the tab to disabled. */
    get disabled(): boolean;
    set disabled(value: BooleanInput);
    private _headerViewRef;
    private _contentViewRef;
    /** @docs-private */
    get headerViewRef(): EmbeddedViewRef<any>;
    /** @docs-private */
    get contentViewRef(): EmbeddedViewRef<any>;
    /**
     * Whether the tab is currently active.
     */
    isActive: boolean;
    constructor(_tabGroup: NxTabGroupBase | null);
    ngOnChanges(changes: SimpleChanges): void;
    ngAfterContentInit(): void;
    ngOnDestroy(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<NxTabComponent, [{ optional: true; host: true; }]>;
    static ɵcmp: i0.ɵɵComponentDeclaration<NxTabComponent, "nx-tab", ["nxTab"], { "label": "label"; "disabled": "disabled"; }, {}, ["templateLabel", "_explicitContent"], ["*"]>;
}
